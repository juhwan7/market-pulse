# Keep provider credentials on the server only. Never expose them to the browser.
# MARKET_DATA_PROVIDER=example
# MARKET_DATA_API_KEY=
# MARKET_DATA_API_SECRET=
# Optional for local development; required for a remotely exposed MCP endpoint.
# MARKET_PULSE_MCP_TOKEN=
# PORT=4173
